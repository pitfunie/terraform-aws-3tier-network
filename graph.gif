                   ┌─────────────┐
                   │ Java Source │
                   └────┬────────┘
                        ↓
             ┌─────────────────────┐
             │ Spring Boot REST App│
             └────┬────────┬───────┘
                  ↓        ↓
           Maven/Gradle   Metadata Endpoint Logic
                  ↓
           Builds app.jar
                  ↓
        ┌────────────────────────┐
        │ Terraform EC2 Resource│
        └────────┬───────────────┘
                 ↓
        EC2 Boot: Install Java + Fetch .jar
                 ↓
       Launch app.jar on port 8080
                 ↓
       ┌────────────────────────────┐
       │ Public IP :8080/metadata   │
       └────────────────────────────┘

